{
  "name": "logicyle-beta-test",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:check": "tsc --noEmit",
    "preview": "vite preview",
    "deploy": "npm run build && echo 'Build terminé ! Vous pouvez maintenant déployer le dossier dist sur Netlify'"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "jspdf": "2.5.1",
    "firebase": "^10.7.0",
    "frappe-charts": "^1.6.2"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "typescript": "~5.3.0",
    "vite": "^5.0.0"
  },
  "overrides": {
    "frappe-charts": {
      "dependencies": {
        "chart.js": "^4.0.0"
      }
    }
  },
  "resolutions": {
    "frappe-charts": "^1.6.2"
  }
}
